[project]
name = "aerial-isaac-lab"
version = "0.1.0"
description = "Aerial Gym dynamics and RL tasks ported to Isaac Lab"
requires-python = ">=3.10"
dependencies = ["torch", "gymnasium"]

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
testpaths = ["tests"]
